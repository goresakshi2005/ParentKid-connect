from django.apps import AppConfig


class HabitBuilderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.habit_builder'
    verbose_name = 'Habit Builder & Scheduler'
